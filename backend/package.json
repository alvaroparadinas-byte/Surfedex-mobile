{
  "name": "surfedex-backend",
  "version": "0.1.0",
  "description": "Backend de Surfedex: sincroniza previsiones desde Stormglass y las sirve al frontend. La clave de Stormglass vive SOLO aquí (variables de entorno).",
  "type": "commonjs",
  "main": "src/server.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node src/server.js",
    "sync": "node src/sync.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "node-cron": "^3.0.3"
  }
}
